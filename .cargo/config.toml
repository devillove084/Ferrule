# CUDA builds use standard Rust host code plus native CUDA/C++ compiled by NVCC.
# Select a target explicitly with FERRULE_CUDA_ARCH or let build.rs detect GPU 0.
[target.'cfg(all())']
rustflags = []
